<%= form_for(physical_person) do |f| %>
  <% if physical_person.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(physical_person.errors.count, "error") %> prohibited this physical_person from being saved:</h2>

      <ul>
      <% physical_person.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cnpj %>
    <%= f.text_field :cnpj %>
  </div>

  <div class="field">
    <%= f.label :nome_fantasia %>
    <%= f.text_field :nome_fantasia %>
  </div>

  <div class="field">
    <%= f.label :person_id %>
    <%= f.text_field :person_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
